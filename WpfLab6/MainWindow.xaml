<Window x:Class="WpfLab6.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WpfLab6"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <Grid>
        <TextBox x:Name="textBox" Text="" FontSize="38" Foreground="Crimson"/>
        <!--<TextBlock>
            <Run Text="Пользователей в команде"/>
            <Run Text="{Binding SelectedTeams.TeamName, Mode=OneWay}"/>  //Пример конкатенации прямо в xaml
            <Run Text=":"/>
            <Run Text="{Binding UsersToTeams.Count, Mode=OneWay}"/>
        </TextBlock>-->
        
    </Grid>
</Window>
